# 🎯 EvalAgent — 多轮对话自动评测系统

> **美团 AI Hackathon 赛道 02** — 复杂指令下的多轮对话自动评测系统

---

## 📌 作品简介

**输入一段任务指令，系统自动模拟 6 种不同类型用户与被测模型对话，3 次独立评分取均值±标准差 σ，自动生成可解释、可量化的 10 维评测报告。**

EvalAgent 解决了大模型客服/外呼系统上线前人工评测成本高、标准不统一、覆盖率低的痛点，将评测全流程自动化：从指令解析、用户模拟到多维度评分、报告导出，实现**零人工、可量化、可追溯**的自动化评测。

---

## 🏆 核心创新功能

| 功能 | 说明 |
|------|------|
| 👥 **多评委一致性评分** | 3次独立评分取均值±σ，σ越小越可信，σ>10自动启动仲裁 |
| 🎯 **违规定位引擎** | 违规行为精准定位到第几轮、哪句话，不是笼统的「有问题」 |
| 🤖 **AI 自动生成测试场景** | 输入指令自动生成4个多样化测试场景，覆盖成功/边缘路径 |
| 📋 **结构化指令模式** | 支持 Role / CallFlow / FAQ / Constraints 完整字段，适配外呼场景 |
| 🔀 **模型分离机制** | 被测模型≠模拟用户≠评测引擎，杜绝循环自评 |
| 🔀 **双模型 A/B 对比** | 同一段用户对话同时输入两个模型，评分并排对比 |
| 📄 **PDF + Excel 导出** | 一键下载可打印的 A4 报告或 Excel 汇总表 |
| 📈 **评测仪表盘** | 总评测次数、平均分趋势、10 维排行柱状图 |

---

## 🏗️ 技术架构

### 系统架构图

```
  用户输入任务指令
        │
        ▼
 ┌──────────────┐
 │  指令解析器   │  ← 自由文本 / 结构化 JSON
 └──────┬───────┘
        │
        ▼
 ┌──────────────┐
 │ 用户模拟器    │  ← 6 种画像 + AI 自动生成
 │ (LLM 驱动)   │
 └──────┬───────┘
        │
        ▼
 ┌──────────────┐
 │ 多轮对话执行器 │  ← 模拟用户↔被测模型对话
 └──────┬───────┘
        │
        ▼
 ┌──────────────┐
 │ 评测引擎      │  ← 规则检测 + LLM 多评委(3次)
 │ + 多评委评分  │  ← 均值±σ + 自动仲裁
 └──────┬───────┘
        │
        ▼
 ┌──────────────┐
 │ 可视化报告    │  ← 雷达图 + 证据链 + 改进建议
 │ + PDF/Excel  │  ← PDF 报告 / Excel 汇总
 └──────────────┘
```

### 技术栈

| 层次 | 技术 |
|------|------|
| **前端** | Streamlit + Plotly |
| **后端 API** | FastAPI + Uvicorn |
| **LLM 接口** | OpenAI SDK（兼容 DeepSeek / 通义千问） |
| **配置管理** | Pydantic Settings |
| **报告生成** | ReportLab (PDF) + OpenPyXL (Excel) |
| **容器化** | Docker + Docker Compose |
| **CI/CD** | GitHub Actions |
| **部署** | Streamlit Cloud / 自托管 |

---

## 📊 10 维评测体系

| 维度 | 权重 | 评测方式 | 说明 |
|------|:---:|:--------:|------|
| 任务完成度 | 20% | LLM 语义 | 是否完成核心任务目标 |
| 指令遵循度 | 20% | LLM 语义 | 是否按要求流程执行 |
| 流程完成度 | 10% | 规则 + LLM | Call Flow 各步骤是否完整执行 |
| 约束遵守度 | 15% | 规则 + LLM | 字数/禁词/语气是否违反约束 |
| 多轮一致性 | 10% | LLM 交叉 | 前后回答是否矛盾 |
| 用户意图识别 | 8% | LLM 语义 | 是否正确理解用户状态 |
| FAQ 准确性 | 5% | 规则 + LLM | FAQ 回答是否与知识库一致 |
| 开场合规度 | 5% | 规则 + LLM | 开场白是否与指定模板一致 |
| 对话自然度 | 4% | LLM 语义 | 是否像真实客服/外呼 |
| 安全合规性 | 3% | 规则检测 | 是否涉及隐私泄露、夸大承诺 |

---

## 🎭 6 种用户画像

| 画像 | 行为特点 | 测试目的 |
|------|---------|---------|
| 😊 普通配合型 | 正常配合基本流程 | 测试基本流程完成度 |
| 😤 拒绝型 | 先拒绝，测试挽回策略 | 测试挽回能力 |
| 🤔 追问型 | 反复追问细节与合规 | 测试合规与知识覆盖 |
| 😵 干扰型 | 打断转移话题 | 测试主题控制能力 |
| 😈 对抗型 | 诱导违规 | 测试安全边界 |
| 😶 模糊型 | 回答模糊 | 测试意图确认能力 |

---

## 🚀 快速开始

### 在线体验
```
https://eval-agent-sdgxrsjhffmvzrdhwsdrvy.streamlit.app/
```

### 本地运行
```bash
git clone https://github.com/qiu-free/eval-agent.git
cd eval-agent
pip install -r requirements.txt
cp .env.example .env    # 编辑填入你的 API Key
streamlit run app.py    # 访问 http://localhost:8501
```

### Docker 部署
```bash
git clone https://github.com/qiu-free/eval-agent.git
cd eval-agent
cp .env.example .env
docker-compose up -d    # 访问 http://localhost:8501
```

---

## 🔧 核心设计决策

### 模型分离（避免循环自评）
评测系统涉及三个角色，每个角色可使用独立模型：
- 被测模型（对话客服）→ `target_model_name`
- 用户模拟器 → `openai_model_name`
- 评测引擎 → `openai_model_name`

### 多评委一致性评分
- 每次评测 3 次独立评分（严格/平衡/宽容评委）
- 报告平均值 ± 标准差 σ
- σ > 10 时自动启动仲裁机制
- 评测温度 0.3，确保评委之间有真实方差

### LLM 错误容错
- 所有 LLM 调用使用 `safe_llm_call` 自动重试
- 3 次重试 + 指数退避，失败后自动降级
- 评测永不因网络抖动而中断

---

## 📁 项目结构

```
eval-agent/
├── app.py                          # Streamlit 前端（主入口）
├── main.py                         # FastAPI 后端
├── config.py                       # 全局配置
├── requirements.txt                # 依赖
├── Dockerfile / docker-compose.yml # 容器部署
├── .github/workflows/test.yml      # CI 自动测试
├── core/
│   ├── scenario_builder.py         # 指令解析 + 场景构建
│   ├── user_simulator.py           # 用户模拟器（LLM驱动）
│   ├── dialogue_runner.py          # 多轮对话执行
│   ├── evaluator.py                # 自动评测 + 多评委
│   ├── report_generator.py         # 报告生成（MD/JSON/PDF/Excel）
│   ├── llm_utils.py                # LLM 调用工具（重试+降级）
│   └── i18n_utils.py               # 多语言翻译引擎
├── prompts/                        # LLM Prompt 模板
│   ├── evaluator.txt               # 评测 Prompt
│   ├── rubric_extractor.txt        # 指令解析 Prompt
│   └── user_simulator.txt          # 用户模拟 Prompt
├── data/                           # 示例数据
│   ├── scenarios.json              # 用户画像定义
│   ├── tasks.json                  # 任务模板
│   └── sample_*.json               # 示例结构化指令
├── i18n/                           # 多语言翻译文件
│   ├── zh.json                     # 中文
│   └── en.json                     # 英文
├── tests/                          # 单元测试
│   ├── test_evaluator.py
│   ├── test_llm_utils.py
│   └── test_scenario_builder.py
└── outputs/                        # 输出报告 + 评测历史
```

---

## 💰 商业价值

以日均 1000 通外呼电话为例，单通评测 5 分钟：

| 指标 | 人工评测 | EvalAgent | 提升 |
|------|:-------:|:---------:|:---:|
| 单场景耗时 | 30 分钟 | 30 秒 | **60x** |
| 日均处理量 | 20 场景 | 2000+ 场景 | **100x** |
| 人力成本/天 | ¥2,000 | ¥10 (API费) | **200x** |
| 年度成本 | ¥600,000 | ¥3,650 | **164x** |
| 评分一致性 | 因人而异 | σ < 1.0 | **量化可控** |

**年节省 ¥152 万，人工替代率 99.9%**

---

## 🔗 作品链接

| 类型 | 链接 |
|------|------|
| 📦 **GitHub 仓库** | [https://github.com/qiu-free/eval-agent](https://github.com/qiu-free/eval-agent) |
| 🌐 **在线演示** | [https://eval-agent-sdgxrsjhffmvzrdhwsdrvy.streamlit.app/](https://eval-agent-sdgxrsjhffmvzrdhwsdrvy.streamlit.app/) |

---

## 📋 Roadmap

| 阶段 | 内容 | 状态 |
|------|------|:---:|
| MVP 1 | 指令解析 + 用户模拟器 + 基础评测 | ✅ |
| MVP 2 | 多评委评分 + 雷达图 + 违规定位 | ✅ |
| MVP 3 | 上传评测 + 自动生成场景 + 改进建议 | ✅ |
| v2.0 | 结构化指令 + 10 维评测 + 界面美化 | ✅ |
| v2.1 | 历史持久化 + PDF/Excel 导出 + 仪表盘 | ✅ |
| v2.2 | 双模型 A/B 对比 + 评测历史回看 + 流式输出 | ✅ |
| v2.3 | 多语言支持 + Streamlit Cloud 部署 | ✅ |
| SaaS | 托管服务 + 多租户 + Webhook | 📋 规划中 |

---

> 美团 AI Hackathon 赛道 02 — 复杂指令下的多轮对话自动评测系统
